// 1) Multiply: an equation whose multiplied results will equal 62
const num1 = 2;
const num2 = 31;
const multiply = num1 * num2; // 2 * 31 = 62

// 2) Random: generates a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Ensures it's an integer > 0

// 3) Mod: an equation that calculates a remainder that is equal to 4
const num3 = 28;
const num4 = 6;
const mod = num3 % num4; // 28 % 6 = 4

// 4) Max: will return 20 as the highest number in the set
const max = Math.max(5, 10, 15, 20); // Ensures 20 is the max value
